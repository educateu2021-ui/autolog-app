import React from 'react'
import ReactDOM from 'react-dom/client'
import AutologApp from './AutologApp.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AutologApp />
  </React.StrictMode>,
)
